<h1>New book</h1>

<%= form_for(@book, url: books_path) do |f| %>
  <% if @book.errors.any? %>
   <div id="error_exp;anatio">
   	<h2><%= @book.errors.count %>errors prohibited this obj from being saved:</h2>
   	<ul>
   	<% @book.errors.full_message.each do |message| %>
   	  <li><% message %></li>
   	<% end %>
   	</ul>
   </div>
	<h4>Title</h4>
	<%= f.text_field :title %>

	<h4>Opinion</h4>
	<%= f.text_area :body %>

	<%= f.submit 'Create Book' %>

<% end %>